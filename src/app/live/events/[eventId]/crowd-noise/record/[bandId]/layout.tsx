import { PublicLayout } from '@/components/layouts'

export default function CrowdNoiseRecordLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <PublicLayout>{children}</PublicLayout>
}
